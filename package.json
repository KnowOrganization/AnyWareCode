{
  "name": "anywherecode",
  "private": true,
  "version": "0.1.0",
  "description": "Discord bot that runs a shared coding agent against your GitHub repos and opens PRs",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "dev": "pnpm --filter @anywherecode/bot dev"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.33.0"
}
